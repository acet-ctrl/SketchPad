class Tool {
    hotKey
    dependency
    result

    constructor(hotKey, dependency, result) {
        this.hotKey = hotKey
        this.dependency = dependency
        this.result = result
    }
}
